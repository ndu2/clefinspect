<UserControl x:Class="ndu.ClefInspect.View.ClefDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ndu.ClefInspect.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:viewmodel="clr-namespace:ndu.ClefInspect.ViewModel.ClefView"
             d:DataContext="{d:DesignInstance Type=viewmodel:ClefViewModel}" >


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding SelectedItem.Time}" />
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="2,0,0,0"
                   Text="{Binding SelectedItem.Level}" Background="{Binding SelectedItem.LevelBackground}" />
        <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" IsReadOnly="True"
                 Text="{Binding SelectedItem.Message, Mode=OneWay}" />
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Text="Exception" FontWeight="Bold" />
        <TextBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                 IsReadOnly="True"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 Text="{Binding SelectedItem.Exception, Mode=OneWay}" />


        <ItemsControl Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
            ItemsSource="{Binding SelectedItemDetails}" Visibility="{Binding DetailsVisibility}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   FontWeight="Bold" Text="{Binding Header}" />
                        <TextBox Grid.Row="1" Grid.Column="0"
                                 IsReadOnly="True"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 Text="{Binding Text, Mode=OneWay}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


        <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Text="Json" FontWeight="Bold" />
        <TextBox Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                                 IsReadOnly="True"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 Text="{Binding SelectedItem.JsonFormatted, Mode=OneWay}" />
    </Grid>
</UserControl>
